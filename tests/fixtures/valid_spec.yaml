openapi: 3.0.1
info:
  title: Valid Test Spec
  version: 1.0.0
paths:
  /test:
    get:
      summary: A test endpoint
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
components:
  schemas:
    TestSchema:
      type: object
      properties:
        id:
          type: integer
